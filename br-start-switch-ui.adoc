---
sidebar: sidebar
permalink: br-start-switch-ui.html
keywords: backing up, back up, backup, backup application data, Amazon Web services, AWS, NetApp Backup and Recovery, Oracle database, Microsoft SQL Server database, SAP HANA database
summary: NetApp Backup and Recovery supports multiple workload types, and some workloads use a different UI. Use the workload switcher to move between them.
---

= Switch to a different NetApp Backup and Recovery workload
:hardbreaks:
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ./media/

[.lead]
NetApp Backup and Recovery supports multiple workload types, and some workloads use a different UI. Use the workload switcher to move between them.

== Switch to a different workload

You can switch to a different workload in the NetApp Backup and Recovery UI.

.Steps
. From the Console left navigation, select *Protection* > *Backup and Recovery*.
. From the top right corner of the page, select the *Switch workload* drop-down list.

. Select the workload that you want to switch to.
+
The page refreshes and shows the selected workload.
