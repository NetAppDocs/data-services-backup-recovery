---
sidebar: sidebar
permalink: br-use-kubernetes-advanced-restore-settings.html
keywords: cr, custom resource, restore, data, backup, advanced, settings
summary: You can customize CR restore operations using annotations, namespace settings, and storage options.
---
= Advanced restore settings for Kubernetes in NetApp Backup and Recovery
:hardbreaks:
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ./media/

[.lead]
Use annotations, namespace settings, and storage options to customize Kubernetes restore operations for your environment.

include::../_include/namespace-anno-labels.adoc[]

== Supported fields
This section describes additional fields for restore operations.

=== Storage class mapping
The `spec.storageClassMapping` attribute maps a storage class in the source application to a new storage class on the target cluster. Use this field when migrating applications between clusters with different storage classes or when changing the storage backend for BackupRestore operations.

*Example:*
[source,yaml]
----
storageClassMapping:
  - destination: "destinationStorageClass1"
    source: "sourceStorageClass1"
  - destination: "destinationStorageClass2"
    source: "sourceStorageClass2"
----

== Supported annotations
This section lists the supported annotations for configuring various behaviors in the system. If you do not explicitly set an annotation, the system uses the default value.

[cols="1,1,3,1", options="header"]
|===
| Annotation | Type | Description | Default value

| protect.trident.netapp.io/data-mover-timeout-sec
| string
| The maximum time, in seconds, that a data mover operation can remain stalled.
| "300"
| protect.trident.netapp.io/kopia-content-cache-size-limit-mb
| string
| The maximum size limit (in megabytes) for the Kopia content cache.
| "1000"
|protect.trident.netapp.io/pvc-bind-timeout-sec
|string
|Maximum time (in seconds) to wait for any newly created PersistentVolumeClaims (PVCs) to reach the `Bound` phase before the operation fails. Applies to all restore CR types (BackupRestore, BackupInplaceRestore, SnapshotRestore, SnapshotInplaceRestore). Use a higher value if your storage backend or cluster often requires more time.
|"1200" (20 minutes)
|===